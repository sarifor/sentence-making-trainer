extends templates/layout

block content
    if records
        table(border="0")
            each record, index in records
                div
                    tr
                        td=index+1
                        td=record.sentence
                        td=record.translated
                        td=record.source
                        td=record.updated                        
    else
        p="Data Missing"


